#lang info
(define collection "detect-os-release")
(define deps '("base" "rackunit-lib"))
(define build-deps '())
(define pkg-desc "Detect the OS the system is running on")
(define version "0.1")
(define pkg-authors '(vdloo))
